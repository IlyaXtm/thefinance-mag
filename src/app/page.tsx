/**
 * thefinance.ir/mag — the listing page.
 *
 * Placeholder. Replaced by the listing implementation (page header, lead
 * block, filter bar, article grid, reports band, newsletter CTA, pagination).
 */
export default function MagHomePage() {
  return (
    <main className="mx-auto px-5 py-[60px] lg:px-[100px] lg:py-24">
      <h1 className="text-3xl font-semibold text-text-primary">مگ فایننس</h1>
      <p className="mt-3 text-text-secondary">
        تحلیل، گزارش و آموزش برای بازارهای مالی
      </p>
    </main>
  );
}
